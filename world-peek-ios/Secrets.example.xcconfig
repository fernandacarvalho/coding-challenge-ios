// Template for Secrets.xcconfig.
// Copy this file to "Secrets.xcconfig" (gitignored) in the same directory
// and replace the placeholder with your real REST Countries API key.
// Secrets.xcconfig is optionally included by Config.xcconfig, so the app
// builds fine even if this file was never copied.
REST_COUNTRIES_API_KEY = REPLACE_WITH_YOUR_KEY
